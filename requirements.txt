# ============================================================
# REQUIREMENTS.TXT
# All the Python packages needed to run this project.
# Install all of them at once by running:
#   pip install -r requirements.txt
# ============================================================

# --- Core AI & Agent Framework ---
langgraph>=0.2.0          # The main library for building our multi-agent graph
langchain>=0.2.0          # Base framework for LLM chains and tools
langchain-community>=0.2.0 # Community tools (FAISS, web search, etc.)
langchain-openai>=0.1.0   # OpenAI LLM integration (using GPT models)

# --- Vector Store (local document storage) ---
faiss-cpu>=1.7.4          # FAISS: stores document embeddings for fast similarity search
# If you prefer ChromaDB instead of FAISS, uncomment the line below:
# chromadb>=0.5.0

# --- Web Search Fallback ---
tavily-python>=0.3.0      # Tavily API: used to search the web when local docs are insufficient
# Alternative: duckduckgo-search>=5.0.0

# --- Streamlit UI ---
streamlit>=1.35.0         # Builds the interactive web interface

# --- Utilities ---
python-dotenv>=1.0.0      # Loads API keys from a .env file safely
tiktoken>=0.7.0           # Tokenizer used by OpenAI models
